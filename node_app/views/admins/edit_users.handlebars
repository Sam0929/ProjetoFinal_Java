 
    <div class='container mt-5'>
        <div class='row justify-content-center'>
            <div class='col-md-8'>
                <div class='card'>
                    <div class='card-header d-flex justify-content-between align-items-center'>
                        <p class="mb-0"> Usuário de Id: {{user.dataValues.id}}
                        <input type="hidden" id="id" name="id" value="{{user.dataValues.id}}">
                         </p>
                        <a class="btn btn-primary ml-auto" href="/" role="button">Voltar</a>  
                    </div>
                        <div class='card-body'> 
                        
                                <form action= "/admin/update/" method="POST">
                                    <div class="mb-3">
                                        <label for="nome" class="form-label">Nome</label>
                                        <input type="text" id = "nome" name="nome" class="form-control"  placeholder="Nome.." value="{{user.dataValues.nome}}">
                                    </div>
                                    <div class="mb-3">
                                        <label for="sobrenome" class="form-label">Sobrenome</label>
                                        <input type="text" id = "sobrenome" name="sobrenome" class="form-control"  placeholder="Sobrenome" value="{{user.dataValues.sobrenome}}">
                                    </div>
                                    <div class="mb-3">
                                        <label for="idade" class="form-label">Idade</label>
                                        <input type="text" id = "idade" name="idade" class="form-control"  placeholder="Idade.." value = "{{user.dataValues.idade}}">
                                        <!--<input id="password" type="password" class="form-control @error('password') is-invalid @enderror" name="password" required autocomplete="new-password">-->
                                    </div>
                                    <div class="mb-3">
                                        <label for="email" class="form-label">Email</label>
                                        <input type="email" id = "email" name="email" class="form-control" placeholder="Email.." autocomplete="email" value = "{{user.dataValues.email}}">
                                    </div>
                                    
                                    <button type='submit' class='btn btn-primary'>Editar</button>

                                </form>
                            
                        </div>
                </div>
            </div>
        </div>
    </div>